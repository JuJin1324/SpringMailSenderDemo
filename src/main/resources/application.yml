spring:
    mail:
        host: smtp.gmail.com
        port: 587
        properties:
            mail:
                smtp:
                    auth: true
                    starttls:
                        enable: true
        username: 'Enter senders email address'
        password: 'Enter password'

logging:
  level:
    root: DEBUG